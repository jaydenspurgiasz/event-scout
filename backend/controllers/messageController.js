import db from "../config/database.js";

export const saveMessage = (eventId, userId, message, database, callback) => {
  // TODO: implement
};

export const getMessages = (eventId, database, callback) => {
  // TODO: implement
};
