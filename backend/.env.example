# Server Configuration
PORT=8000
NODE_ENV=development

# JWT Secret
JWT_SECRET=your-secret-key-change-in-production

# Client URL for CORS (comma-separated for multiple origins)
CLIENT_URL=http://localhost:3000

# Email Reminders (optionally get free test credentials at https://ethereal.email)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=leo8@ethereal.email
SMTP_PASS=YTpaAhnf7TnKWQ26yA
SMTP_FROM="Event Scout" <noreply@eventscout.com>
